@model Asp_projekt.Models.KolegijCreateViewModel
@using Asp_projekt.Models
@{
    ViewData["Title"] = "Novi kolegij";
}

<section class="detail-hero">
    <span class="hero-badge">Kolegij</span>
    <div class="hero-main">
        <h1 class="hero-title">Novi kolegij</h1>
        <p class="hero-subtitle">Unesi podatke o novom kolegiju.</p>
    </div>
</section>

<section class="detail-grid">
    <article class="detail-card core-card">
        <header class="card-header">
            <h2>Podaci</h2>
            <span class="card-tag">Create</span>
        </header>

        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly"></div>

            <div class="info-grid">
                <div class="info-item">
                    <label class="info-label" asp-for="Naziv">Naziv</label>
                    <input asp-for="Naziv" />
                    <span asp-validation-for="Naziv"></span>
                </div>

                <div class="info-item">
                    <label class="info-label" asp-for="ECTS">ECTS</label>
                    <input asp-for="ECTS" type="number" min="1" max="30" />
                    <span asp-validation-for="ECTS"></span>
                </div>

                <div class="info-item">
                    <label class="info-label" asp-for="FakultetId">Fakultet</label>
                    <select asp-for="FakultetId">
                        <option value="">Bez fakulteta</option>
                        @foreach (var fakultet in Model.Fakulteti)
                        {
                            if (Model.FakultetId == fakultet.Id)
                            {
                                <option value="@fakultet.Id" selected>@fakultet.Naziv</option>
                            }
                            else
                            {
                                <option value="@fakultet.Id">@fakultet.Naziv</option>
                            }
                        }
                    </select>
                    <span asp-validation-for="FakultetId"></span>

                    @{
                        var fakultetQuickPickModel = new AutocompleteDropdownModel
                        {
                            ComponentId = "fakultetQuickPickKolegijCreate",
                            InputId = "fakultetQuickPickInputKolegijCreate",
                            EndpointUrl = Url.Action("Autocomplete", "Fakultet") ?? string.Empty,
                            Label = "Brzi odabir fakulteta",
                            Placeholder = "Upisi barem 2 znaka",
                            MinChars = 2
                        };
                    }
                    <partial name="_AutocompleteDropdown" model="fakultetQuickPickModel" />
                </div>
            </div>

            <div class="inline-links inline-links--spaced">
                <button class="link-btn" type="submit">Spremi</button>
                <a class="link-btn link-btn--ghost" asp-action="Index">Odustani</a>
            </div>
        </form>
    </article>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        (function () {
            const fakultetSelect = document.getElementById('FakultetId');
            const fakultetQuickPick = document.getElementById('fakultetQuickPickKolegijCreate');

            fakultetQuickPick?.addEventListener('autocomplete:selected', function (event) {
                const selectedId = event?.detail?.value;
                if (!fakultetSelect || selectedId == null || selectedId === '') {
                    return;
                }

                fakultetSelect.value = String(selectedId);
                fakultetSelect.dispatchEvent(new Event('change'));
            });
        })();
    </script>
}
